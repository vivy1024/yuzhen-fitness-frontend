# 安装指南

**版本**: v1.0.0  
**更新日期**: 2025-01-02  
**状态**: ✅ 已完成

---

## 📦 克隆项目

```bash
# 克隆仓库
git clone <repository-url>

# 进入前端目录
cd yuzhen_fitness
```

---

## 🔧 安装依赖

### 使用pnpm (推荐)

```bash
# 安装依赖
pnpm install

# 或使用快捷命令
pnpm i
```

### 使用npm

```bash
npm install
```

### 使用yarn

```bash
yarn install
```

---

## ⚙️ 环境变量配置

### 1. 复制环境变量模板

```bash
cp .env.example .env
```

### 2. 编辑 `.env` 文件

```bash
# API地址（根据实际情况修改）
VITE_API_BASE_URL=http://localhost:8000
VITE_DAML_RAG_API_URL=http://localhost:8001

# 应用配置
VITE_APP_TITLE=玉珍健身
VITE_APP_VERSION=1.7.0

# 功能开关
VITE_ENABLE_PWA=true
VITE_ENABLE_MOCK=false
```

### 3. 环境变量说明

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `VITE_API_BASE_URL` | Laravel后端API地址 | `http://localhost:8000` |
| `VITE_DAML_RAG_API_URL` | DAML-RAG服务地址 | `http://localhost:8001` |
| `VITE_APP_TITLE` | 应用标题 | `玉珍健身` |
| `VITE_ENABLE_PWA` | 是否启用PWA | `true` |
| `VITE_ENABLE_MOCK` | 是否启用Mock数据 | `false` |

---

## 🚀 启动开发服务器

```bash
# 启动开发服务器
pnpm dev

# 或指定端口
pnpm dev --port 9000
```

服务器启动后，访问: http://localhost:9000

---

## 🏗️ 构建生产版本

```bash
# 构建生产版本
pnpm build

# 预览生产版本
pnpm preview
```

构建产物位于 `dist/` 目录。

---

## 🧪 运行测试

```bash
# 运行单元测试
pnpm test

# 运行测试并生成覆盖率报告
pnpm test:coverage

# 运行E2E测试
pnpm test:e2e
```

---

## 🔍 代码检查

```bash
# 运行ESLint检查
pnpm lint

# 自动修复ESLint问题
pnpm lint:fix

# 运行TypeScript类型检查
pnpm type-check
```

---

## 📱 PWA相关

### 生成PWA图标

```bash
# 生成各种尺寸的PWA图标
pnpm generate-pwa-icons
```

### 测试PWA功能

1. 构建生产版本: `pnpm build`
2. 启动预览服务器: `pnpm preview`
3. 在Chrome中打开: http://localhost:4173
4. 打开DevTools → Application → Service Workers
5. 检查Service Worker是否注册成功

---

## 🐛 常见问题

### 1. 依赖安装失败

**问题**: `pnpm install` 报错

**解决方案**:
```bash
# 清除缓存
pnpm store prune

# 删除node_modules和lock文件
rm -rf node_modules pnpm-lock.yaml

# 重新安装
pnpm install
```

### 2. 端口被占用

**问题**: `Port 9000 is already in use`

**解决方案**:
```bash
# 方案1: 使用其他端口
pnpm dev --port 9001

# 方案2: 杀死占用端口的进程 (Windows)
netstat -ano | findstr :9000
taskkill /PID <PID> /F

# 方案2: 杀死占用端口的进程 (Mac/Linux)
lsof -ti:9000 | xargs kill -9
```

### 3. TypeScript类型错误

**问题**: 类型检查报错

**解决方案**:
```bash
# 重新生成类型声明
pnpm type-check

# 重启VS Code的TypeScript服务器
# 在VS Code中: Ctrl+Shift+P → TypeScript: Restart TS Server
```

### 4. 热更新不生效

**问题**: 修改代码后页面不自动刷新

**解决方案**:
```bash
# 重启开发服务器
# Ctrl+C 停止服务器
pnpm dev
```

---

## 📚 下一步

安装完成后，继续阅读 [快速开始](./快速开始.md)

---

**维护者**: 薛小川  
**最后更新**: 2025-01-02
